# Review Risk Radar — 전체 동작 플로우

> 사용자가 대시보드를 열었을 때부터 경보가 화면에 표시되기까지의 흐름을 시나리오로 설명한다.

---

## 등장인물

| 역할 | 설명 |
|------|------|
| 사용자 | 대시보드를 보는 사람 |
| 프론트엔드 | React 앱 (localhost:5173) |
| 백엔드 | FastAPI 서버 (localhost:8000) |
| 파이프라인 | Processor → Detector → AlertGenerator |

---

## 1단계 — 서버 시작 (백엔드)

`uvicorn main:app --reload` 명령어를 실행하면:

```
shoes_sample.json (28,000건)
        ↓
_normalize_columns()       ← review_date → timestamp 변환, product_title → title 변환
        ↓
RiskRadar(df)              ← 파이프라인 초기화
        ↓
✅ RiskRadar 초기화 완료 | 상품 수: 100
```

이후 API 요청을 받을 준비가 된 상태로 대기한다.

---

## 2단계 — 대시보드 최초 진입 (프론트엔드)

사용자가 브라우저에서 `http://localhost:5173`을 열면:

```
브라우저
  ↓
GET /api/meta
  ↓
백엔드 응답:
{
  "products": [
    { "product_id": "B01HGVY0TM", "title": "Merrell Women's Moab 2...", "review_count": 396 },
    ...
  ],
  "date_range": { "min": "2018-01-01", "max": "2023-12-31" },
  "attributes": ["사이즈", "내구성", "착용감", "디자인", "반품"]
}
```

프론트엔드는 이 정보로:
- 상품 선택 드롭다운을 채운다
- 날짜 선택기의 범위를 `2018-01-01 ~ 2023-12-31`로 제한한다
- 속성 선택 드롭다운을 채운다

---

## 3단계 — 사용자가 상품과 날짜를 선택

> **시나리오**: 사용자가 "Merrell Women's Moab 2"를 선택하고, 기준 날짜를 `2023-12-31`, 윈도우를 `90일`로 설정한다.

```
사용자 조작
  ↓
GET /api/alerts/B01HGVY0TM?reference_date=2023-12-31&window_days=90
GET /api/trend/rating/B01HGVY0TM
```

두 요청이 동시에 발생한다.

---

## 4단계 — 경보 파이프라인 실행 (백엔드 핵심)

`/api/alerts` 요청이 들어오면 `RiskRadar.scan()`이 실행된다.

```
reference_date = 2023-12-31
window_days    = 90

current  = 2023-10-02 ~ 2023-12-31 리뷰  (최근 90일)
previous = 2023-07-04 ~ 2023-10-02 리뷰  (이전 90일)
```

### 4-1. 키워드 감지 (KeywordSpikeDetector)

```
current 리뷰 텍스트 분석
  → "sole" 포함 리뷰 비율: 현재 15%, 이전 5%  → 증가율 +200% → 위험 🔴
  → "blister" 포함 리뷰 비율: 현재 8%, 이전 6% → 증가율 +33%  → 위험 🔴
  → "ugly" 포함 리뷰 비율: 현재 2%, 이전 2%   → 증가율 0%    → 무시
```

### 4-2. 평점 감지 (RatingTrendDetector)

```
current 평균 평점:  3.67
previous 평균 평점: 4.60
하락폭: 0.93점 → 위험 🔴
```

### 4-3. 경보 생성 (AlertGenerator)

```
KeywordSpikeEvent → RiskAlert (위험, 내구성, +200%)
KeywordSpikeEvent → RiskAlert (위험, 착용감, +33%)
RatingDropEvent   → RiskAlert (위험, 전체 평점, ▼0.93점)
        ↓
위험도 순으로 정렬 후 반환
```

---

## 5단계 — 화면에 경보 표시 (프론트엔드)

백엔드 응답을 받은 프론트엔드는:

```
경보 목록
┌─────────────────────────────────────────┐
│ 🔴 위험  내구성                          │
│ 'sole' 관련 부정 언급 +200% 증가 (최근 90일) │
│ [증거 리뷰 보기 ▼]                       │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│ 🔴 위험  착용감                          │
│ 'blister' 관련 부정 언급 +33% 증가       │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│ 🔴 위험  전체 평점                       │
│ 평점 4.60 → 3.67 (▼0.93점, 최근 90일)   │
└─────────────────────────────────────────┘
```

요약 뱃지도 업데이트된다:

```
[ 위험: 3 ]  [ 경고: 0 ]  [ 양호: 0 ]
```

---

## 6단계 — 시계열 차트 표시

경보와 별개로 두 개의 차트가 로드된다.

```
GET /api/trend/rating/B01HGVY0TM
  → 월별 평균 평점 데이터 (2018-01 ~ 2023-12)
  → 프론트: 꺾은선 차트로 렌더링

GET /api/trend/keyword/B01HGVY0TM/사이즈
  → 월별 '사이즈' 속성 부정 키워드 언급 비율
  → 프론트: 꺾은선 차트로 렌더링 (속성 변경 가능)
```

---

## 전체 흐름 요약

```
사용자 접속
    ↓
GET /api/meta → 상품 목록, 날짜 범위, 속성 목록
    ↓
사용자: 상품 선택 + 기준 날짜 설정
    ↓
GET /api/alerts  →  current/previous 윈도우 슬라이싱
                 →  키워드 급증 감지
                 →  평점 하락 감지
                 →  위험도 분류
                 →  경보 카드 표시
GET /api/trend/rating   → 평점 시계열 차트
GET /api/trend/keyword  → 키워드 비율 시계열 차트
```

---

## 응답 시간

매 요청마다 파이프라인을 처음부터 실행하지만, 데이터가 메모리에 상주하므로 응답은 빠르다.

| 엔드포인트 | 응답 시간 |
|-----------|---------|
| `/api/meta` | ~7ms |
| `/api/alerts` | ~6ms |
| `/api/trend/rating` | ~6ms |
| `/api/trend/keyword` | ~8ms |

---

## 기준 날짜를 바꾸면?

기준 날짜를 `2021-06-01`로 바꾸면 파이프라인은 2021년 기준으로 다시 실행된다.

```
current  = 2021-03-03 ~ 2021-06-01 리뷰
previous = 2020-12-03 ~ 2021-03-03 리뷰
```

이를 통해 "2021년 초에 이 시스템을 운영했다면 언제 경보가 울렸을지"를 재현할 수 있다.  
정적 데이터를 실시간 모니터링처럼 시뮬레이션하는 핵심 아이디어다.
